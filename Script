const user = $('AI Goal Planner & Scheduler').first().json;

const aiResponse =
  $json.text ||
  $json.output ||
  JSON.stringify($json);

return [
  {
    json: {
      title: user['title'],
      email: user['Email'],
      examDate: user['Exam Date'],
      availableHours: user['Study Hours Per Day'],
      plan: aiResponse
    }
  }
];
